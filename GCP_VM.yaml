- name: Launch GCP Instance via Jenkins
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    gcp_project: "qwiklabs-gcp-02-6ab56494f7c6"
    gcp_zone: "us-east5-a"             # FIXED: allowed zone
    machine_type: "e2-medium"
    instance_name: "rucha-jenkins-ansible-vm"
    gcp_cred_file: "/var/lib/jenkins/GCP_KEY"

  tasks:
    - name: Create GCP instance
      google.cloud.gcp_compute_instance:
        name: "{{ instance_name }}"
        machine_type: "{{ machine_type }}"
        disks:
          - auto_delete: true
            boot: true
            initialize_params:
              source_image: projects/debian-cloud/global/images/family/debian-11
        network_interfaces:
          - network:
              selfLink: "global/networks/default"
            access_configs:
              - name: External NAT
                type: ONE_TO_ONE_NAT
        zone: "{{ gcp_zone }}"
        project: "{{ gcp_project }}"
        auth_kind: serviceaccount
        service_account_file: "{{ gcp_cred_file }}"
        state: present
      register: instance_data

    - name: Display Instance IP
      debug:
        msg: "Instance Created. Public IP: {{ instance_data.networkInterfaces[0].accessConfigs[0].natIP }}"
